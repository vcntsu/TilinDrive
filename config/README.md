# Esta carpeta contiene las credenciales privadas de Google Cloud.
# Los archivos credentials.json y token.json están excluidos del repositorio
# por seguridad (ver .gitignore). NO subir esos archivos a GitHub.
